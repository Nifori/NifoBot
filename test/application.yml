spring:
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
    datasource:
        url: "jdbc:h2:mem:testdb;MODE=MySQL;DATABASE_TO_LOWER=TRUE"
        username: "sa"
        password: "sa"
        driver-class-name: org.h2.Driver